const path = require('path')
const inspect = require('inspect.js')

describe.only('FireError', () =>
  describe('class', () =>
    it('replace origin of errors in a call stack', () =>
      try
        throw new Error('FireError test')
      catch (err)
        console.log(err.stack)
    )
  )
)
