import * as inspect from 'inspect.js'
import ** as __FS from '../'

describe('__FS', () =>
  describe('class', () =>
    it('should return a static class', () =>
      inspect(__FS).isObject()
    )

    it('should have a typing method', () =>
      inspect(__FS.typing).isFunction()
    )
  )
)
