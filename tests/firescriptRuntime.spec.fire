import inspect from'inspect.js'
import FirescriptRuntime from '../'

describe('FirescriptRuntime', () =>
  describe('class', () =>
    it('should return a static class', () =>
      inspect(FirescriptRuntime).isCalss()
